{
  "name": "@oscarr/backend-runtime",
  "version": "0.7.2",
  "private": true,
  "type": "module",
  "description": "Minimal manifest for the bundled prod image. Only lists modules that stay external in the esbuild bundle (native + Prisma) — everything else is inlined in dist/server.js.",
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "prisma": "^6.19.3",
    "bcrypt": "^6.0.0",
    "bare-fs": "^4.7.1",
    "bare-os": "^3.9.0",
    "bare-path": "^3.0.0",
    "bare-url": "^2.4.2"
  },
  "overrides": {
    "picomatch": ">=4.0.4"
  }
}
